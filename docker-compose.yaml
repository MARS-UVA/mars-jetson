services:
  gstreamer:
    build:
      context: .
      dockerfile: docker/gstreamer.Dockerfile
    network_mode: host
    user: "1000:1000"
    environment:
      - ROS_DOMAIN_ID=0
      - CONTROL_STATION_IP="172.25.183.75"
      - FASTDDS_BUILTIN_TRANSPORTS=UDPv4
